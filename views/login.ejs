<%- include('./partials/header') %>

    <% if(error.length>0){ %>
        <div class="absolute top-24 left-1/2 -translate-x-1/2 p-3 rounded-md bg-red-500 z-50 animate-bounce">
            <span class="inline-block text-white font-medium">
                <%= error %>
            </span>
        </div>
        <% } %>

            <div class="flex w-full min-h-[calc(100vh-5rem)] bg-white">
                <!-- Decorative Left Panel -->
                <div
                    class="hidden md:flex w-1/2 bg-zinc-900 flex-col justify-center px-16 text-white relative overflow-hidden">
                    <div class="relative z-10">
                        <h1 class="text-6xl font-bold tracking-tighter mb-4 leading-none">Welcome<br>Back.</h1>
                        <p class="text-zinc-400 text-lg">Sign in to continue your journey with Scatch.</p>
                    </div>
                    <div
                        class="absolute top-0 left-0 w-full h-full bg-gradient-to-br from-zinc-800 to-zinc-950 opacity-50 z-0">
                    </div>
                </div>

                <!-- Login Form Section -->
                <div class="w-full md:w-1/2 flex items-center justify-center p-8">
                    <div class="w-full max-w-md">
                        <h2 class="text-3xl font-bold mb-2 text-zinc-800">Login</h2>
                        <p class="text-zinc-500 mb-8">Enter your details to access your account.</p>

                        <form autocomplete="off" action="/users/login" method="post" class="space-y-4">
                            <div>
                                <input
                                    class="w-full px-4 py-3 rounded-lg border border-zinc-200 bg-zinc-50 focus:bg-white focus:border-blue-500 focus:ring-2 focus:ring-blue-100 outline-none transition-all placeholder:text-zinc-400"
                                    type="email" placeholder="Email Address" name="email">
                            </div>
                            <div>
                                <input
                                    class="w-full px-4 py-3 rounded-lg border border-zinc-200 bg-zinc-50 focus:bg-white focus:border-blue-500 focus:ring-2 focus:ring-blue-100 outline-none transition-all placeholder:text-zinc-400"
                                    type="password" placeholder="Password" name="password">
                            </div>
                            <input
                                class="w-full py-3 rounded-full bg-blue-600 text-white font-medium hover:bg-blue-700 transition cursor-pointer shadow-lg shadow-blue-500/30"
                                type="submit" value="Sign In">
                        </form>

                        <div class="mt-8 text-center text-zinc-500 text-sm">
                            Don't have an account? <a href="/register"
                                class="text-blue-600 font-medium hover:underline">Create Account</a>
                        </div>
                    </div>
                </div>
            </div>
            <%- include('./partials/footer') %>